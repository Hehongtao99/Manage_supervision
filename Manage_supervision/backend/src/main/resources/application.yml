server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://115.120.221.163:3306/student_manager?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull
    username: root
    password: hht500234
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          time_zone: Asia/Shanghai

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 应用自定义配置
app:
  upload:
    dir: uploads  # 文件上传目录

jwt:
  secret: your_jwt_secret_key_here_please_change_in_production
  expiration: 86400000  # 24小时 